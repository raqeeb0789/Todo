Server
PORT=5000
MONGODB_URI=mongodb://localhost:27017/todo-app
JWT_SECRET=change_this_secret
JWT_EXPIRES_IN=7d

Frontend
CLIENT_URL=http://localhost:5173

Email (SMTP) - set these for real emails
EMAIL_HOST=smtp.yourprovider.com
EMAIL_PORT=587
# If using port 465, secure will auto-enable. Otherwise explicitly set true/false:
EMAIL_SECURE=false
EMAIL_USER=your_smtp_username
EMAIL_PASS=your_smtp_password
EMAIL_FROM="Todo App <no-reply@yourdomain.com>"
# Optional: set to false to allow self-signed certs (dev/test only)
EMAIL_TLS_REJECT_UNAUTH=true
